# Weather_Dashboard
A command-line weather dashboard written in pure C that:
1. Fetches weather from an online API
2. Parses JSON
3. Saves data into SQLite
4. Reads configuration from a file
5. Displays weather nicely in terminal
6. Uses logging + error handling
